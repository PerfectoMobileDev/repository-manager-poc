'use strict';

module.exports.handle = (event, context, callback) => {
    // create a response
    const response = {
      statusCode: 200,
      body: JSON.stringify(event),
    };
    callback(null, response);
}